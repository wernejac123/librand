# librand
An experimental library I'm creating for generating random numbers
By default it compiles as a shared library (.so file in Linux)
This was tested on a x86_64 machine using Slackware 15.0.

Note: there is a seperate Makefile for test.c. It may need to be altered
too if this library is installed in a diferent location or as a static binary.
